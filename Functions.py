def functionOne(localvariable):# this is a function that stores a string of data 
  localvariable="hello"#
  print (localvariable)# this prints the variable 

def functionTwo(parameter):# this is a function that stores the parameter 
  print (parameter)# this prints the parameter

def functionThree(globalNumericvalue):# this is a function that stores a global variable 
  global globalNumericvalue
  globalNumericvalue = globalNumericvalue + 2
  print(globalNumericvalue)# this prints the global variable 
